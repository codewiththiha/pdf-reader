[package]
name = "pdf-storage"
version = "0.5.0"
edition = "2024"
description = "Key-value persistence abstraction for the PDF reader."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
web-sys = { version = "0.3", features = ["Window", "Storage"] }
