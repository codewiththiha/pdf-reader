[build]
target = "./index.html"

[watch]
# styles.css is generated by the tailwind pre_build hook — ignoring it prevents
# an infinite rebuild loop (tailwind writes styles.css -> trunk rebuild -> hook -> ...).
# Do NOT ignore styles/input.css: edits to the theme should trigger a rebuild.
ignore = ["./src-tauri", "./styles.css"]

# Compile Tailwind v4 (CSS-first) before every trunk build (dev + prod).
[[hooks]]
stage = "pre_build"
command = "npx"
command_arguments = ["@tailwindcss/cli", "-i", "styles/input.css", "-o", "styles.css", "--minify"]

[serve]
port = 1420
open = false
