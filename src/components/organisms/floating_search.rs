//! Floating search overlay. Stub only — the body is filled in a later phase; it
//! must simply compile with zero warnings until then.

use leptos::prelude::*;

use crate::core::state::AppState;

#[allow(dead_code)] // consumed in phase 2
#[component]
pub fn FloatingSearch(state: AppState) -> impl IntoView {
    // Param intentionally unused until the overlay body lands.
    let _ = state;
    view! {}
}
