/dist/
/target/

# macOS
.DS_Store

# Claude config (may contain local secrets)
.claude/

# Generated by the tailwind pre_build hook
/styles.css

# Generated by the tsc pre_build hook from public/pdfEngine.ts and scripts/*.ts
# (Trunk runs the hook on every build; `npm run build:ts` regenerates them
# locally; CI runs `npm ci && npm run build:ts` before any test step.)
/public/pdfEngine.js
/public/engine/*.js
/scripts/*.js

# Node
/node_modules/

# Local-only tooling
/.script/
/.env
CONTRACTS.md
contracts.md
bun.lock
.st.txt
.claude-shell-test.txt

# Local web build output (wasm-bindgen webgen + assembled static root)
/webgen/
/webroot/
